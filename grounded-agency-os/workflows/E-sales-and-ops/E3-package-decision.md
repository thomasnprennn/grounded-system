# E3 — Package Decision

## Purpose

IF unclear problem → Foundation Audit. IF concrete system problem + commitment → Brand System Build. IF execution across touchpoints → Full Implementation. IF scope unclear → scope mapping call.

## Output

Save relevant messages, decisions, and next steps inside:

`/clients/[client]/messages/`
`/clients/[client]/approvals/`
`/clients/[client]/03-decisions.md`

## Quality Gate

- one clear next step
- no pressure language
- no vague pricing
- package matched to problem
- client decision documented
